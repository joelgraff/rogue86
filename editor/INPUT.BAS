1 LOCATE 1,1
2 PRINT "DRAW.BAS"
3 MERGE "DRAW.BAS"
1000 '----------------------
1001 '---Input Processing---
1002 '----------------------
1010 A$ = INKEY$
1012 IF A$ = "" THEN RETURN
1014 KEY.PRESS = ASC(A$)
1016 IF KEY.PRESS = 0 THEN KEY.PRESS = ASC(RIGHT$(A$,1)) + 200
1020 CMD.ACTIVE = CMD.MAP(KEY.PRESS)
1022 IF CMD.ACTIVE AND CMD.MOV THEN GOTO 3000
1024 IF CMD.ACTIVE AND CMD.ATR THEN GOTO 6000
1028 IF CMD.ACTIVE AND CMD.KPD THEN GOTO 5000
1030 IF CMD.ACTIVE AND CMD.KBD THEN GOTO 5000
1040 GOTO 5400
1099 RETURN
1500 '------ EXTENDED KEY PRESS
1510 A$ = ""
1520 WHILE A$ = ""
1530   A$ = INKEY$
1550   IF A$ = "Q" THEN QUIT.LOOP = 1
1560   IF A$ = "S" THEN GOTO 4000
1570   IF A$ = "L" THEN GOTO 4200
1990 WEND
1999 RETURN
